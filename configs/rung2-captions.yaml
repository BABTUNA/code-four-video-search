# Ablation rung 2: + VLM captions (the vocabulary-we-control index).
data_dir: data
retrieval:
  modalities: [transcript, speaker_turn, caption]
  frames: false
  audio: false
planner: {enabled: true, model: openai/gpt-5-nano}
verifier: {enabled: false}
