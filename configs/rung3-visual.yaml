# Ablation rung 3: + frame embeddings, objects, scenes, on-screen clock.
data_dir: data
retrieval:
  modalities: [transcript, speaker_turn, caption, frame, object, scene, wall_clock]
  audio: false
planner: {enabled: true, model: google/gemini-3.5-flash-lite}
verifier: {enabled: false}
