# Ablation rung 1: transcripts only - the "what deployed tooling does" baseline.
data_dir: data
retrieval:
  modalities: [transcript]
  frames: false
  audio: false
planner: {enabled: true}
verifier: {enabled: false}
