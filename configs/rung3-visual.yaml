# Ablation rung 3: + frame embeddings, objects, scenes, overlay clock.
data_dir: data
retrieval:
  modalities: [transcript, caption, frame, object, wall_clock]
  audio: false
planner: {enabled: true}
verifier: {enabled: false}
